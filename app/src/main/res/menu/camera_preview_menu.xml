<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/actionRetakeButton"
        android:icon="@drawable/ic_baseline_retake_camera_48"
        android:title="@string/retake_camera_menu_item_title"
        app:showAsAction="ifRoom"
        android:iconTint="@color/white"/>

    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/actionDeleteButton"
        android:icon="@drawable/ic_baseline_delete_48"
        android:title="@string/delete_camera_menu_item_title"
        app:showAsAction="ifRoom"
        android:iconTint="@color/white"/>
</menu>